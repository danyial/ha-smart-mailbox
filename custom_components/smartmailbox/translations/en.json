{
  "config": {
    "step": {
      "user": {
        "title": "Smart Mailbox Setup",
        "description": "Configure your smart mailbox sensors.",
        "data": {
          "flap_entity": "Flap sensor (mail slot)",
          "door_entity": "Door sensor (retrieval door)",
          "debounce_seconds": "Debounce time (seconds)",
          "notify": "Enable push notifications",
          "notify_service": "Notification service(s)"
        },
        "data_description": {
          "notify_service": "Comma-separated list for multiple services (e.g. notify.mobile_app_iphone, notify.telegram)"
        }
      }
    },
    "abort": {
      "already_configured": "Smart Mailbox is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Smart Mailbox Options",
        "data": {
          "flap_entity": "Flap sensor (mail slot)",
          "door_entity": "Door sensor (retrieval door)",
          "debounce_seconds": "Debounce time (seconds)",
          "notify": "Enable push notifications",
          "notify_service": "Notification service(s)",
          "notify_message": "Notification message",
          "enable_counter": "Enable delivery counter",
          "enable_age": "Enable mail age sensor",
          "age_unit": "Age unit",
          "reset_on_empty": "Reset counter when emptied"
        },
        "data_description": {
          "notify_service": "Comma-separated list for multiple services (e.g. notify.mobile_app_iphone, notify.telegram)",
          "notify_message": "📬 New mail in the mailbox!",
          "age_unit": "Display mail age in hours or days"
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "post": {
        "name": "Mail"
      }
    },
    "sensor": {
      "last_delivery": {
        "name": "Last delivery"
      },
      "last_emptied": {
        "name": "Last emptied"
      },
      "delivery_counter": {
        "name": "Delivery counter"
      },
      "mail_age": {
        "name": "Mail age"
      }
    },
    "button": {
      "reset_counter": {
        "name": "Reset counter"
      },
      "mark_empty": {
        "name": "Mark as empty"
      }
    }
  },
  "services": {
    "reset_counter": {
      "name": "Reset counter",
      "description": "Resets the delivery counter to zero.",
      "fields": {
        "entry_id": {
          "name": "Config entry ID",
          "description": "Optional: Specify which mailbox instance to reset."
        }
      }
    },
    "mark_empty": {
      "name": "Mark as empty",
      "description": "Marks the mailbox as empty (no mail present).",
      "fields": {
        "entry_id": {
          "name": "Config entry ID",
          "description": "Optional: Specify which mailbox instance to mark as empty."
        }
      }
    }
  }
}
